import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ryaa.demo',
  appName: 'ryaa-use-issue-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
